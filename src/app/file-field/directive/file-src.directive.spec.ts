import { FileSrcDirective } from './file-src.directive';

describe('FileSrcDirective', () => {
  it('should create an instance', () => {
    // const directive = new FileSrcDirective();
    // expect(directive).toBeTruthy();
  });
});
